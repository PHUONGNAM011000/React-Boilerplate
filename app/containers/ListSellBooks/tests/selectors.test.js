// import { selectListSellBooksDomain } from '../selectors';

describe("selectListSellBooksDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});

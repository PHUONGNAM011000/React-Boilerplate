/*
 * FormTest Messages
 *
 * This contains all the text for the FormTest container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.FormTest";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FormTest container!"
  }
});
